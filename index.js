console.log(`
x-ember-cli is meant to be used as a cli application.
It's used globally. Just run \`x-ember-cli\` and pass any commands you need that would be run by the ember-cli`);

console.log(`
Note that x-ember-cli is limited.
Use for only the purposes intended,
and only use if your version of ember-cli is close to the version listed in the peer dependencies of this package`);
